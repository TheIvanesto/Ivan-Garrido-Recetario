{
  "name": "recetario-web",
  "version": "1.0.0",
  "description": "Recetario SPA (vanilla) con integración a TheMealDB y json-server mock API",
  "scripts": {
    "start": "live-server --port=5500",
    "server-mock": "json-server --watch db.json --port 3000",
    "check-index": "node -e \"const fs=require('fs'); process.exit(fs.existsSync('index.html')?0:1)\""
  },
  "devDependencies": {
    "json-server": "^0.17.3",
    "live-server": "^1.2.2"
  }
}
